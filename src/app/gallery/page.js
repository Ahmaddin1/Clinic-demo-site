import GalleryGrid from "@/components/GalleryGrid";

export const metadata = {
  title: "Clinic Gallery | Dental Avenue Faisalabad",
  description:
    "Take a look inside Dental Avenue Faisalabad -- our treatment rooms, waiting area, equipment, and team. Modern, clean, and welcoming.",
};

export default function GalleryPage() {
  return <GalleryGrid />;
}
